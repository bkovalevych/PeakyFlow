function scrollYByVh() {
    //const clientHeight = document.documentElement.clientHeight;
    //window.scrollBy(0, clientHeight);

    const starter = document.querySelector(".starter");
    starter.classList.add("starter-collapsed");
}